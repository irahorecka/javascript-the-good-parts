// CHAPTER 2 -- GRAMMAR


// the ternary operator
// if first operand is truthy, it produces the value of the second, else third
var x = 1;
var y = 2;

console.log(x > y ? x : y);  // will return 2 in this case.
